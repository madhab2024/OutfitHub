{
  "name": "multi-vendor-clothing-platform",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "shared",
    "apps/admin",
    "apps/customer",
    "apps/logistic"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:admin dev:customer dev:logistic",
    "dev:backend": "npm run dev -w backend",
    "dev:admin": "npm run dev -w admin",
    "dev:customer": "npm run dev -w customer",
    "dev:logistic": "npm run dev -w logistic",
    "build": "npm-run-all build:shared build:backend build:admin build:customer build:logistic",
    "build:shared": "npm run build -w @platform/shared",
    "build:backend": "npm run build -w backend",
    "build:admin": "npm run build -w admin",
    "build:customer": "npm run build -w customer",
    "build:logistic": "npm run build -w logistic"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}